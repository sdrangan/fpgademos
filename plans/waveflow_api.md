# waveflow API surface (Step 2 output)

Auto-generated by introspecting the **installed** waveflow package, so it
reflects the *current* API to migrate toward — not the old pysilicon snapshot.

- **Generated:** 2026-08-05 (supersedes the 2026-06-23 snapshot).
- **Source:** editable install of `../pysilicon` (the waveflow repo; remote
  `github.com/sdrangan/waveflow.git`), at branch `sweep-runner`, commit
  `b7d9059`, clean tree. `origin/main` was at `108501b`.
- **Installed version:** `waveflow-0.1.0` (no `__version__` attribute exposed).
- **Regenerate with:**
  `..\hwdesign-venv\Scripts\python.exe plans/introspect_waveflow.py out.md`
  (see [`introspect_waveflow.py`](introspect_waveflow.py)), then re-attach this
  hand-written header above the `---` separator.

> ⚠️ **This snapshot tracks a moving target.** waveflow is installed *editable*
> from a clone you actively develop in, so `import waveflow` resolves to
> whichever branch is checked out there — the branch changed during the session
> that produced this file. If something in this document doesn't match reality,
> check `git -C ../pysilicon branch --show-current` before debugging further,
> and regenerate.

## What changed since the 2026-06-23 snapshot

waveflow roughly **doubled**: 41 → **89** documented modules. The additions
cluster in four areas, none of which existed (or were this developed) in June:

- **`waveflow.calib.*` — 24 new modules.** An entire calibration subsystem:
  `resource_model`, `timing_model`, `vitis_model`, `bus_model`, `record_store`,
  `platform`, `module_key`, `synth_report`, `confidence`, `device_rules`,
  plus a CLI (`waveflow_calib`) and `publish` entry point.
- **`waveflow.mcp.*` — 11 new modules.** LLM/MCP tooling: `server`, `registry`,
  `example_rag`, `schema_tools`, `file_tools`, `build_corpus`, `headless`.
- **`waveflow.build.*` — 8 new steps/modules:** `elaborate`, `sweep`,
  `codegen_dispatch`, `codegen_check`, `composite_gen`, `calib_steps`,
  `resource_steps`, `trace_steps`.
- **`waveflow.hw.*` — the component model was rewritten.**
  `waveflow.hw.component` and `waveflow.hw.hw_component` are **gone**, replaced
  by `hw_module`, `hw_freerun`, `hw_hostactivated`, `hw_state`, `hw_testbench`.
  Also new: `mem_stream`. Any June-era notes referencing `hw_component` are dead.
- Smaller: `waveflow.utils.burst_io`, `waveflow.utils.trace`;
  `waveflow.simulation` fully enumerated (`simobj`, `simulation`, `logger`,
  `stream_tb`).

**Correction to the June snapshot:** it claimed `waveflow.*` re-exports the
subpackages (`build`, `calib`, `hw`, …). It does not — `waveflow/__init__.py`
is **empty**. The June listing was an artifact of the introspection script
importing submodules before enumerating the top level. Every import must be
deep and explicit: `from waveflow.hw.dataschema import DataArray`.

## Migration-relevant observations

- **No top-level re-exports** (see correction above). Imports are deep, e.g.
  `from waveflow.hw.dataschema import DataArray`, `from waveflow.utils import
  fixputils` — never `from waveflow import DataArray`.
  (The June version of this file argued this justified a `hwdesign/compat.py`
  shim. That was **rejected** — see `plans/migration.md` Step 5: integration is
  per-unit and bespoke, so a uniform re-export shim is the wrong abstraction.)
- **The old pysilicon → waveflow module mapping still holds** — all 16 target
  modules below were re-verified against this snapshot on 2026-08-05:
  - `pysilicon.utils.fixputils`   → `waveflow.utils.fixputils`
  - `pysilicon.utils.timing`      → `waveflow.utils.timing`
  - `pysilicon.utils.vcd`         → `waveflow.utils.vcd`
  - `pysilicon.utils.csynthparse` → `waveflow.utils.csynthparse`
  - `pysilicon.hw.dataschema`     → `waveflow.hw.dataschema`
  - `pysilicon.hw.arrayutils`     → `waveflow.hw.arrayutils`
  - `pysilicon.hw.memory`         → `waveflow.hw.memory`
  - `pysilicon.toolchain.toolchain` → `waveflow.toolchain.toolchain`
  - `pysilicon.build.streamutils` → `waveflow.build.streamutils`
  - `pysilicon.scripts.{xsim_vcd,sv_sim,sv_build,collect_overlay,build_lab_autograder,create_doc_assets}`
    → same names under `waveflow.scripts.*`
  - **Verify per-symbol, not just per-module** — signatures/behavior have evolved
    (e.g. fixed-point is now a `DataArray`/`FixedField` schema system, build is a
    `BuildDag`/`BuildStep` graph). The module exists ≠ the old call still works.
- **Console scripts** (13, all verified present in the venv after reinstall):
  `xsim_vcd`, `sv_sim`, `sv_synth`, `sv_impl`, `collect_overlay`,
  `build_lab_autograder`, `create_doc_assets`, `clean_examples`, `build_corpus`,
  `publish_calib`, `waveflow_calib`, `waveflow_mcp_setup`, `waveflow_mcp_server`.
  `autograde_llm_latex` is **deleted** (retired). `pysilicon_copy` has **no**
  waveflow equivalent → retired (the vendoring model it served is gone).
- **New surface area not in old pysilicon** — do not migrate *to* these unless a
  unit actually needs them: `waveflow.calib.*`, `waveflow.mcp.*`,
  `waveflow.build.*` (codegen/DAG/sweep), `waveflow.hw.*` synth IR
  (`hwstmt`, `hw_module`, `regmap`, `memif`, `interface`), `waveflow.simulation`.

---

waveflow package dir: `C:\Users\sdran\Documents\repos\pysilicon\waveflow`

## Top-level `waveflow` namespace

_(no `__all__` defined)_

Public names re-exported at `waveflow.*`:

### `waveflow.build.build` — build.py — build configuration, pipeline primitives, and artifact generation.

**Classes:**
- `BuildArtifact` — Base for any value produced by a BuildStep.  _(methods: is_fresh_relative_to)_
- `BuildConfig` — Configuration for a Waveflow build.  _(methods: needs_legacy_streamutils_cpp, vitis_version_tuple)_
- `BuildDag` — A directed acyclic graph of :class:`BuildStep` nodes.  _(methods: add, artifact_owners, artifact_paths, describe, info, results_status, run, step_names, steps)_
- `BuildResult` — Outcome of a single :class:`BuildStep` execution.  _(methods: object, path)_
- `BuildStep` — Abstract base for any node in a build DAG.  _(methods: expected_paths, run)_
- `Buildable` — A :class:`BuildStep` that declares named file outputs and generates their  _(methods: build_outputs, generate, is_fresh, run)_
- `FileArtifact` — A file written to disk.  Freshness is checked via the file's mtime.  _(methods: is_fresh_relative_to)_
- `ObjectArtifact` — An in-memory Python object (sim result, DataFrame, parsed report, …).  _(methods: is_fresh_relative_to)_
- `SourceStep` — A source file that serves as a root dependency in the build graph.  _(methods: expected_paths, produces, run)_

**Functions:**
- `source_artifact(path: 'Path') -> 'FileArtifact'` — Wrap a source file as a FileArtifact using its mtime as the timestamp.

### `waveflow.build.calib_steps` — calib_steps.py — the DAG steps that drive timing calibration (both levels).

**Classes:**
- `CalibBusStep` — Calibrate the **platform** bus-transfer model from a traced run's ``m_axi`` ports.  _(methods: consumes, produces, run)_
- `CollectTimingStep` — Append one run's RTL + pysim measurements to every attached model's corpus.  _(methods: consumes, produces, run)_
- `FitTimingStep` — Fit every attached model from its accumulated corpus, writing each ``params.json``.  _(methods: consumes, produces, run)_
- `SeedPlatformStep` — Make sure this build's platform exists, seeding it from an upstream one the first time.  _(methods: run)_

### `waveflow.build.cli` — Shared introspection CLI runner for ``BuildDag`` examples.

**Functions:**
- `run_dag_cli(dag_factory: 'Callable[[], BuildDag]', *, description: 'str', default_through: 'str', root_dir: 'Path', extra_args: 'Iterable[tuple[tuple, dict]]' = (), params_from_args: 'Callable[[argparse.Namespace], dict] | None' = None) -> 'None'` — Run an example's ``BuildDag`` with the standard introspection CLI.

### `waveflow.build.codegen_check` — codegen_check.py — ``check(source, target)``: *would this lower?*, as a predicate.

**Functions:**
- `check(source, target: 'str | None' = None) -> 'tuple[bool, str | None]'` — Would *source* lower to *target*?  Returns ``(True, None)`` or ``(False, message)``.
- `potential_targets(source) -> 'frozenset[str]'` — The codegen targets that **exist for** *source*'s kind.

### `waveflow.build.codegen_dispatch` — codegen_dispatch.py — typed, class-based codegen dispatch.

**Classes:**
- `CodegenPath` — How a component's kind lowers to codegen.

**Functions:**
- `codegen_path(comp) -> 'CodegenPath'` — Return the :class:`CodegenPath` for *comp*, dispatched on its class.

### `waveflow.build.composite_gen` — composite_gen.py — the graph-derived free-running (``ap_ctrl_none``) ``hls::task`` top generator.

**Classes:**
- `BfmInst` — One model construction in the generated testbench: ``<cls> <name>(dut, "<prefix>", <args>);``
- `BfmModel` — The XSI testbench model a participant lowers to — the testbench twin of :class:`TaskInst`.
- `ExtPort` — One external interface port of the generated top: its C++ param decl + interface pragmas.  _(methods: xsi_prefix)_
- `FramedEdge` — A **framed** internal edge -> ``hls_thread_local hls::stream<streamutils::framed_word<W> >``.  _(methods: decl)_
- `IntChannel` — One internal channel of the generated top: its ``hls_thread_local`` decl + what it connects.
- `SobEdge` — A block (SOBIF) internal edge -> ``hls_thread_local hls::stream_of_blocks<T[N], 2>`` (the  _(methods: decl)_
- `StreamEdge` — A stream internal edge (the default) -> ``hls_thread_local hls::stream<ap_uint<W> >``.  _(methods: decl)_
- `TaskInst` — One ``hls::task`` instantiation inside the top: the templated body + its call args.  _(methods: inst_name)_
- `TbSpec` — A generated XSI testbench harness, derived from a testbench composite's graph.
- `TopSpec` — A generated free-running (``ap_ctrl_none``) ``hls::task`` top.  For a standalone kernel there  _(methods: internal_streams, trace_manifest)_

**Functions:**
- `bundle_map(boundary) -> 'dict[str, str]'` — Assign an ``m_axi`` bundle to each memory port: **gmem0, gmem1, ... in declaration order.**
- `composite_top_spec(comp, width: 'int' = 64) -> 'TopSpec'` — Derive the composite :class:`TopSpec` from *comp*'s component/interface graph.
- `derive_boundary(comp, names) -> 'tuple[tuple[str, object], ...]'` — Pair *names* with *comp*'s boundary endpoints, **derived from the component graph**.
- `derive_internal_edges(comp) -> 'list'` — The internal edges of *comp*, **derived from the interfaces it registered with ``add_if``**.
- `kind_of_endpoint(ep) -> 'str'` — The boundary kind an endpoint lowers to, **derived from its type**.
- `render_ports_h(spec: 'TopSpec') -> 'str'` — Emit ``<top>_ports.h`` — the testbench's port binding, derived from *spec*.
- `render_rtl_f(top_name: 'str', root) -> 'str'` — Emit the ``xvlog`` file list (``rtl_<top>.f``) for *top*'s elaborated RTL.
- `render_tb_harness(spec: 'TbSpec') -> 'str'` — Emit ``<top>_tb_harness.h`` — the testbench's participants, phases and fixed-N loop.
- `render_tb_main(spec: 'TbSpec', n_cycles: 'int') -> 'str'` — Emit ``<top>_bfm_tb.cpp`` — the whole TB ``main``, now derivable from the graph.
- `render_tcl(top_name: 'str', extra_sources: 'tuple[str, ...]' = (), *, part: 'str' = 'xc7z020clg484-1', period_ns: 'float' = 10) -> 'str'` — Emit a csynth ``.tcl`` for ``vitis-run --mode hls --tcl`` (concrete width baked in, so the
- `render_top(spec: 'TopSpec') -> 'str'` — Emit the generated free-running ``ap_ctrl_none`` ``hls::task`` top .cpp for *spec*.
- `render_vectors_h(ns: 'str', scalars=None, arrays=None, note: 'str' = '') -> 'str'` — Emit a plain-C++ header of generated test vectors: ``<ns>.h``.
- `tb_top_spec(tb) -> 'TbSpec'` — Derive the XSI testbench harness from *tb*'s component/interface graph.
- `tcl_target(config) -> 'tuple[str, float]'` — The ``(part, period_ns)`` a csynth TCL should pin, taken from *config*'s resolved platform.

### `waveflow.build.cosim_steps` — BuildSteps that bridge the Vitis HLS cosim report into the build DAG.

**Classes:**
- `ExtractCosimTimingStep` — Read a Vitis cosim report and emit a structured ``cosim_timing`` JSON.  _(methods: consumes, produces, run)_
- `ValidateTimingStep` — Compare Python-side and cosim-side transaction cycle counts.  _(methods: consumes, produces, run)_

### `waveflow.build.elaborate` — elaborate.py — the elaboration contract.

**Classes:**
- `ElabContext` — A sim-free stand-in for :class:`Simulation`, used only to *elaborate*.  _(methods: env, run_sim)_
- `ParamPurityError` — Raised when a component's structure is not a pure function of its params.

**Functions:**
- `assert_param_pure(comp_class: 'type', params: 'dict[str, Any] | None' = None) -> 'None'` — Assert *comp_class*'s structure is a pure function of *params*.
- `elaborate(comp_class: 'type', params: 'dict[str, Any] | None' = None, *, name: 'str' = '_codegen', check_purity: 'bool' = True) -> 'Any'` — Elaborate *comp_class* with *params* and return the built structure.
- `structure_signature(comp: 'Any') -> 'Any'` — A canonical, name/identity-agnostic structural signature of *comp*.

### `waveflow.build.hwcodegen` — HwStmtExtractor — build-time AST → HwStmt IR converter.

**Classes:**
- `HwStmtExtractor` — Parse ``run_proc`` of an ``HwModule`` into an ``HwStmt`` tree.  _(methods: extract)_
- `SynthesisError` — Raised when ``run_proc`` contains a non-synthesizable pattern.

**Functions:**
- `extract_kernel(comp) -> 'HwStmt'` — Extract the kernel body and return a fully-resolved ``HwStmt`` tree.
- `extract_testbench(comp) -> 'HwStmt'` — Extract the testbench ``main()`` body and return a resolved ``HwStmt`` tree.

### `waveflow.build.hwcodegen_steps` — BuildStep wrappers for HLS codegen.

**Classes:**
- `HlsCodegenStep` — Generate ``<component>.hpp``, ``<component>.cpp``, and one impl stub per hook.  _(methods: consumes, produces, run)_
- `TaskBodyStep` — Generate a composite **task body** ``<name>_task.h`` + one impl stub per hook.  _(methods: consumes, produces, run)_

### `waveflow.build.hwgen` — Walk a resolved ``HwStmt`` tree and emit C++ source as a string.

**Classes:**
- `CodegenCtx` — CodegenCtx(comp: 'HwModule', params: 'dict[str, str]' = <factory>, endpoint_names: 'dict[int, str]' = <factory>, indent: 'int' = 1, stream_flavor: 'str' = 'axi4s')  _(methods: child, pad)_
- `TbCodegenCtx` — Codegen context for the testbench-mode emitter.  _(methods: child, pad)_

**Functions:**
- `cpp_kernel_name(comp_class) -> 'str'` — Default kernel function name.
- `cpp_type(typ) -> 'str'` — Translate a Python type or ``HwVar.typ`` marker to its HLS C++ representation.
- `derive_kernel_task(comp)` — Derive a :class:`KernelTask` for a leaf whose body this module GENERATES.
- `header_to_cpp(comp_class, output_dir: 'str' = '.', impl_dir: 'str | None' = None) -> 'str'` — Build the content of ``<component>.hpp``.
- `hook_signature(method, template_params: 'list[str] | None' = None) -> 'tuple[str, list[tuple[str, str]]]'` — Return ``(return_cpp_type, [(arg_name, arg_decl), ...])`` for a hook.
- `hook_signature_str(method, template_params: 'list[str] | None' = None) -> 'str'` — Return the full C++ declaration string for a hook (no body, no semicolon).
- `hook_template_params(stmt: 'FunctionStmt') -> 'list[str]'` — Return the ordered, deduplicated ``HwParam`` names this hook is templated on.
- `impl_stub_to_cpp(comp, hook_method, header_name: 'str | None' = None) -> 'str'` — Build the first-time stub content for one hook impl file.
- `impl_stub_to_tpp(comp, hook_method, template_params: 'list[str]') -> 'str'` — Build the first-time stub content for one templated hook's ``.tpp`` file.
- `kernel_body_to_cpp(comp: 'HwModule') -> 'str'` — Top-level driver: extract + resolve + codegen.
- `kernel_files_to_str(comp_class, output_dir: 'str' = '.', impl_dir: 'str | None' = None) -> 'dict[str, str]'` — Top-level driver. Returns ``{filename: contents}`` for all generated files.
- `kernel_signature(comp, variant_suffix: 'str' = '') -> 'str'` — Build the concrete kernel function signature + ``#pragma HLS INTERFACE`` lines.
- `kernel_to_cpp(comp_class) -> 'str'` — Build the content of ``<component>.cpp``.
- `resolved_namespace(comp_class) -> 'str | None'` — Return the C++ namespace to use for this component's hooks.
- `state_decl_type(entry) -> 'tuple[str, str]'` — Return ``(cpp_type, suffix)`` for one :class:`~waveflow.hw.hw_state.HwState`'s declaration.
- `state_decls_to_cpp(comp: 'HwModule', indent: 'int' = 1) -> 'str'` — Emit the ``static`` declarations for every object *comp* registered with ``add_state``.
- `state_pragmas(entry, pad: 'str' = '') -> 'list[str]'` — The HLS pragma lines one :class:`~waveflow.hw.hw_state.HwState` declares.
- `task_files_to_str(comp_class, task_name: 'str | None' = None) -> 'dict[str, str]'` — Emit a composite **task body** for *comp_class* — the ``mem_seq_task.h`` product.
- `task_signature(comp, ctx: 'CodegenCtx', task_name: 'str') -> 'str'` — Build a composite **task body** signature — the ``mem_seq_task.h`` shape.
- `task_template_params(comp, ctx: 'CodegenCtx') -> 'list[str]'` — The ordered, deduplicated C++ template-param names a task body is templated on.
- `tb_files_to_str(tb_class, output_dir: 'str' = '.') -> 'dict[str, str]'` — Build the file set for a ``HwTestbench`` subclass.
- `tb_to_cpp(stmt: 'HwStmt', ctx: 'TbCodegenCtx') -> 'str'` — Emit C++ source for a testbench-mode statement.
- `to_cpp(stmt: 'HwStmt', ctx: 'CodegenCtx') -> 'str'` — Emit C++ source for a statement (and its children). Returns a string.

### `waveflow.build.hwresolve` — Resolve raw AST nodes in an ``HwStmt`` tree to real Python values.

**Classes:**
- `ResolutionError` — Raised when an AST node in the IR cannot be resolved to a value.

**Functions:**
- `resolve_kernel(tree: 'HwStmt', comp: 'HwModule') -> 'HwStmt'` — Walk *tree* and replace AST nodes in inputs/values with real values.
- `resolve_testbench(tree: 'HwStmt', comp) -> 'HwStmt'` — Resolve a testbench-extractor tree (entry point ``main``).

### `waveflow.build.resource_steps` — resource_steps.py — the build step that turns a synthesis report into per-module resource records.

**Classes:**
- `InspectSynthStep` — Attribute a csynth report to modules, write it as JSON, and file the records.  _(methods: run)_

### `waveflow.build.streamutils`

**Classes:**
- `MemMgrStep` — Build step that copies the memory-manager headers to an output directory.  _(methods: build_outputs, generate, output_dir)_
- `MemStreamStep` — Build step that copies the fixed ``MemRStream`` / ``MemWStream`` task-body headers to an  _(methods: build_outputs, generate, output_dir)_
- `StreamUtilsStep` — Build step that copies the streamutils support files to an output directory.  _(methods: build_outputs, generate, output_dir, run)_
- `XsiHarnessStep` — Build step that copies the XSI testbench harness into an example's ``xsi/`` directory.  _(methods: build_outputs, generate, output_dir)_

### `waveflow.build.sweep` — sweep.py — run a design at every point of a parameter grid, and keep the measurements.

**Classes:**
- `ParamGrid` — The points a sweep visits: a Cartesian product over named axes, in **declaration order**.  _(methods: build_axes, label, subset, workload_axes)_

### `waveflow.build.trace_steps` — BuildSteps for the timing-trace rung: name the nets, run the RTL, extract the timing.

**Classes:**
- `AddVcdTopStep` — Write ``vcd_dumper_<top>.v`` -- the second elaborated top that dumps the waveform.  _(methods: consumes, produces, run)_
- `ExtractBurstsStep` — Turn a traced run into a **timing table**: one row per component firing.  _(methods: consumes, produces, run)_
- `RtlSimStep` — Run the RTL through XSI **with tracing on**, producing ``<top>_trace.vcd``.  _(methods: consumes, produces, run)_
- `TraceManifestStep` — Write ``<top>_trace.json`` -- the RTL net names for this component's channels and ports.  _(methods: consumes, produces, run)_

### `waveflow.build.verify_steps` — Functional verification BuildSteps.

**Classes:**
- `FunctionalVerifyStep` — Compare a ``golden_dir`` artifact against an ``actual_dir`` artifact.  _(methods: consumes, produces, run)_

### `waveflow.calib.bus_model` — bus_model.py — platform-scoped m_axi bus-transfer calibration.

**Classes:**
- `BusCalib` — Fit + persist a platform's per-direction bus-transfer span model.  _(methods: add_run, bus_timing, fit, path, points_dir)_

**Functions:**
- `measure_bus_span(bt, bundle: 'str', direction: 'str', idle_gap: 'int' = 8) -> 'dict | None'` — Measure a ``m_axi`` bundle's **per-transfer** bus span as a ``{num_trans, nwords, span}``

### `waveflow.calib.calib` — calib.py — bare-bones calibration corpus + model fitting.

**Classes:**
- `CalibDataFrame` — A table of calibration datapoints backed by a ``pandas.DataFrame`` (:attr:`df`).  _(methods: add_datapoint, extend, load, save)_
- `CalibModel` — Base per-target calibration model: predict ``target`` from a ``basis`` of columns.  _(methods: confidence, confidence_feat, corpus, corpus_df, corpus_markdown, corpus_path, corpus_row, data_dir, default_model, design, estimate, fit, get_params, has_free_params, holdout_report, load_model, load_or_default, load_params, max_rel_error, n_free_params, params_path, predict, predict_feat, rel_errors, save_model, score, target_column, targets, to_params, transform, uncovered)_
- `ConcatCalibModel` — Several models composed **across targets** -- each answers for the targets it covers.  _(methods: confidence_feat, fit, get_params, has_free_params, load_params, n_free_params, predict_feat, sub_models, targets, to_params)_
- `InterpCalibModel` — Piecewise-linear interpolation over a **single** basis column — a calibrated  _(methods: fit, from_samples, load_params, n_free_params, samples, to_params)_
- `LinCalibModel` — Linear least-squares model over the :attr:`basis` columns, backed by  _(methods: as_dict, coeffs, design, fit, load_params, n_free_params, plot, to_params, transform)_
- `LookupCalibModel` — Return the measurement for exactly this feature point; refuse to interpolate.  _(methods: confidence_feat, fit, has_free_params, load_params, n_free_params, predict_feat, to_params)_
- `PriorCalibModel` — Targets computed from a **formula** over the features.  No free parameters.  _(methods: confidence_feat, fit, has_free_params, load_params, n_free_params, predict_feat, targets, to_params, transform)_

### `waveflow.calib.cli` — cli.py — ``waveflow_calib``: create, inspect and publish a calibration platform.

**Functions:**
- `cmd_list(args) -> 'int'` — Every platform visible from here, in resolution order, with where each one comes from.
- `cmd_new(args) -> 'int'` — Create a platform directory, optionally seeded from an existing one.
- `cmd_publish(args) -> 'int'`
- `cmd_show(args) -> 'int'` — Inventory a platform: identity, bus law, component residuals, module records.
- `main(argv: "'list[str] | None'" = None) -> 'int'`

### `waveflow.calib.confidence` — confidence.py — what a calibrated model knows about its own prediction.

**Classes:**
- `Confidence` — A level plus whatever the model wants to say about why.  _(methods: summary, to_json, uncalibrated)_
- `ConfidenceError` — A :class:`Confidence` was built with a non-serializable fact, or an unbacked ``EXACT`` claim.
- `ConfidenceLevel` — How much a prediction should be believed.  Ordered; see :attr:`rank`.  _(methods: rank)_
- `Estimate` — A predicted value together with where it came from and how much to believe it.  _(methods: level, to_json)_
- `FitSummary` — The little that must be retained from a fit for a *deployed* model to still report confidence.  _(methods: covers, degenerate, from_json, is_exact, outside, to_json)_

### `waveflow.calib.device_rules` — device_rules.py — the device physics a resource prior stands on, in one place.

**Classes:**
- `BramEstimate` — What one partitioned array costs, and how much to believe it.  _(methods: certain)_
- `BramGeometry` — The shape of one hard memory block.
- `Device` — The primitives one part family is built from.
- `DeviceMismatchError` — A model calibrated on one device is being asked to price another.
- `DspGeometry` — The multiplier ports of one hard DSP primitive.
- `UnknownPartError` — No device rule is registered for a part.

**Functions:**
- `bram_count(n_banks: 'int', depth: 'int', elem_bits: 'int', part: "'str | None'" = None) -> 'int'` — :func:`bram_estimate`'s block count.  ``uncertain`` still returns the block-RAM figure.
- `bram_estimate(n_banks: 'int', depth: 'int', elem_bits: 'int', part: "'str | None'" = None) -> 'BramEstimate'` — Blocks for *n_banks* banks of *depth* elements each, and whether they are blocks at all.
- `bram_shape(elem_bits: 'int', part: "'str | None'" = None) -> "'tuple[int, int]'"` — ``(blocks_per_element, entries_per_block)`` for an array of *elem_bits* elements.
- `device_for(part: "'str | None'" = None, *, strict: 'bool' = False) -> 'Device'` — The :class:`Device` a part is built from.
- `dsp_count(n_mult: 'int', operand_bits: 'int', part: "'str | None'" = None) -> 'int'` — DSPs for *n_mult* multiplies of *operand_bits* operands.
- `dsp_per_mult(operand_bits: 'int', part: "'str | None'" = None) -> 'float'` — DSPs consumed by one signed ``operand_bits x operand_bits`` multiply.
- `require_same_device(part: "'str | None'", measured_on: "'str | None'", *, what: 'str' = 'model')` — Raise :class:`DeviceMismatchError` unless *part* and *measured_on* are the same device.

### `waveflow.calib.fixture` — fixture.py — the per-component calibration fixture contract and registry.

**Classes:**
- `ComponentFixture` — A reusable infra component's timing-calibration harness.  _(methods: calibrate, rtl_firings, run_pysim, sweep, timing_model)_
- `SweepPoint` — One point in a fixture's calibration grid — the structural facts of a firing.

**Functions:**
- `all_fixtures() -> "'list[ComponentFixture]'"` — Every registered fixture, in registration order.
- `get(component: 'str') -> 'ComponentFixture'` — The registered fixture for *component*, or a ``KeyError`` naming what is registered.
- `register(fixture: 'ComponentFixture') -> 'ComponentFixture'` — Register a fixture instance under its :attr:`~ComponentFixture.component` id.  Returns it, so a

### `waveflow.calib.fixtures.mem_r_stream` — mem_r_stream.py — the calibration fixture for the in-band forwarding reader.

**Classes:**
- `MemRStreamFixture` — Calibration fixture for the in-band forwarding reader.  _(methods: rtl_firings, run_pysim, sweep)_

### `waveflow.calib.fixtures.mem_w_stream` — mem_w_stream.py — the calibration fixture for the in-band forwarding writer.

**Classes:**
- `MemWStreamFixture` — Calibration fixture for the in-band forwarding writer.  _(methods: rtl_firings, run_pysim, sweep)_

### `waveflow.calib.migrate_keys` — migrate_keys.py — re-address a record store after a deliberate signature change.

**Classes:**
- `KeyMigration` — The old -> new key mapping for one store, and what is wrong with it.  _(methods: ok, report)_

**Functions:**
- `apply_migration(store: 'ModuleStore', mig: 'KeyMigration', *, dry_run: 'bool' = True) -> 'list'` — Rename each module directory to its new key and rewrite the identity inside it.
- `plan_migration(store: 'ModuleStore', designs: "'list[tuple[type, dict, str]]'") -> 'KeyMigration'` — Work out how a store's keys move, without touching it.
- `store_root(platform_dir: "'str | Path'") -> 'Path'`

### `waveflow.calib.module_key` — module_key.py — the content-addressed identity of a calibratable module.

**Classes:**
- `ModuleIdentity` — What a calibration record is *about*: one module class in one resolved configuration.  _(methods: from_json, to_json)_
- `UnboundModuleError` — A module was keyed before its ports were bound, so its structure is not yet fully determined.
- `UnstableSignatureError` — A module's structure signature is not reproducible across processes.

**Functions:**
- `assert_bound(comp: 'Any', exempt: "'frozenset | set'" = frozenset()) -> 'None'` — Raise :class:`UnboundModuleError` if any of *comp*'s endpoints has an unresolved depth.
- `boundary_endpoint_ids(top: 'Any') -> 'frozenset'` — The ``id()`` of every endpoint on *top*'s declared boundary, or empty if it has none.
- `config_id(kernel_task: 'Any') -> 'str'` — The **configuration-qualified** id of a task: its function name plus its template arguments.
- `identify(comp_class: 'type', params: 'dict[str, Any] | None' = None, *, require_bound: 'bool' = False) -> 'ModuleIdentity'` — Elaborate *comp_class* with *params* and return its :class:`ModuleIdentity`.
- `identify_instance(comp: 'Any', *, require_bound: 'bool' = True, exempt: "'frozenset | set'" = frozenset()) -> 'ModuleIdentity'` — Build a :class:`ModuleIdentity` from an **already elaborated** module.
- `module_key(comp_class: 'type', params: 'dict[str, Any] | None' = None, *, require_bound: 'bool' = False) -> 'str'` — The short content-addressed key for ``(comp_class, params)`` — e.g. ``fir_compute-1f0a3c7d``.
- `resolved_params(comp: 'Any') -> 'dict[str, Any]'` — The **resolved** ``HwParam`` values on an elaborated *comp* — declared defaults included.
- `signature_digest(comp: 'Any') -> 'str'` — The full hex SHA-256 of *comp*'s structure signature.
- `snake_name(cls_name: 'str') -> 'str'` — ``FirCompute`` -> ``fir_compute`` — the human-readable half of a key.
- `walk_modules(top: 'Any', *, include_top: 'bool' = True) -> "'list[tuple[str, Any, ModuleIdentity]]'"` — Walk an elaborated *top* and return ``[(path, module, identity), ...]`` depth-first.

### `waveflow.calib.platform` — platform.py — the per-platform calibration library.

**Classes:**
- `Platform` — A named calibration platform: a directory plus the ``part`` + ``clk_freq`` its fit is valid for.  _(methods: calib, check_counters, component_dir, manifest_path, res_types, resolve, synth_period_ns)_
- `PlatformCalib` — The shared calibration library for one platform — the home for the bus law and every reusable  _(methods: bus, component_dir)_
- `PlatformMismatchError` — A selected platform's stored ``part`` / clock does not match the build's, and the build did not
- `PlatformMismatchWarning` — Emitted (instead of raised) when a platform's part/clock differs from the build's but
- `UnknownCounterError` — A model named a resource counter the platform does not measure in.

**Functions:**
- `packaged_platforms_dir() -> 'Path | None'` — The read-only reference platforms shipped inside the ``waveflow`` package, or ``None`` if they
- `platform_fallback_path() -> 'list[Path]'` — The read-only roots to search *after* a build's own ``platforms_root``, most-preferred first.
- `user_platforms_dir() -> 'Path'` — The per-user, writable platform library — where a user calibrating a platform that did not

### `waveflow.calib.publish` — publish.py — promote calibration from an untracked work dir into the tracked platform library.

**Classes:**
- `FileAction` — One publishable artifact and what publishing it would do.
- `Guard` — A coverage-regression check for one calibration *unit* (the bus, or one component): how many  _(methods: regressed)_
- `PublishPlan` — PublishPlan(work_dir: 'Path', tracked_dir: 'Path', actions: 'list[FileAction]' = <factory>, guards: 'list[Guard]' = <factory>)  _(methods: changed, regressions)_
- `RegressionError` — Raised by :func:`apply_plan` when publishing would replace a fit with a thinner one (fewer

**Functions:**
- `apply_plan(plan: 'PublishPlan', *, force: 'bool' = False) -> 'list[str]'` — Write the changed artifacts into the tracked dir; return the rel paths actually written.
- `build_plan(work_dir: 'str | Path', tracked_dir: 'str | Path') -> 'PublishPlan'` — Compute what publishing *work_dir* into *tracked_dir* would do — a pure inspection, no writes.
- `main(argv: 'list[str] | None' = None) -> 'int'`
- `seed_platform(src_dir: 'str | Path', dst_dir: 'str | Path', *, force: 'bool' = False) -> 'list[str]'` — Copy an existing platform's stable artifacts into *dst_dir* as a starting point.

### `waveflow.calib.record_store` — record_store.py — the per-module measurement store: one envelope for timing and resources.

**Classes:**
- `KeyCollisionError` — Two structurally different modules resolved to the same short key.
- `ModuleStore` — The ``modules/`` tier of a platform's calibration library.  _(methods: append, best, coverage, get_identity, keys, module_dir, put_identity, read, records_path, root, total_cost_seconds)_
- `Provenance` — What a record was measured *from* — the basis on which reusing it is safe.  _(methods: from_json, to_json)_
- `Record` — One measurement about one module configuration.  _(methods: fidelity, from_json, to_json)_
- `StaleRecordError` — A stored record does not describe the module it was read for.

**Functions:**
- `corpus_from_records(store: "'ModuleStore'", *, cls_name: 'str | None' = None, target: 'str' = 'resource', source: 'str | None' = None, counters: 'Iterable[str] | None' = None, payload_keys: 'Iterable[str] | None' = None)` — Reduce a store's records to the canonical corpus — the resource axis's raw-tier generator.
- `integration_record(identity: 'ModuleIdentity', integration: 'dict', *, source: 'str', boundary: 'dict | None' = None, part: 'str' = '', period_ns: 'float' = 0.0, tool: 'str' = '', cost_seconds: 'float' = 0.0, measured_at: 'str | None' = None) -> 'Record'` — Build an ``target="integration"`` :class:`Record` — the composite's own cost.
- `merge_payloads(records: 'Iterable[Record]') -> 'dict[str, int]'` — Sum the resource counters across *records* — the raw Σ-modules term.
- `normalize_resources(raw: 'dict') -> 'dict[str, int]'` — Coerce a parsed csynth resource dict into plain ints under canonical counter names.
- `resource_record(identity: 'ModuleIdentity', resources: 'dict', *, source: 'str', part: 'str' = '', period_ns: 'float' = 0.0, tool: 'str' = '', cost_seconds: 'float' = 0.0, extra: 'dict | None' = None, measured_at: 'str | None' = None) -> 'Record'` — Build a ``target="resource"`` :class:`Record` from a parsed report's resource dict.
- `with_cost(record: 'Record', seconds: 'float') -> 'Record'` — *record* with its ``cost_seconds`` set — the timing of a run is usually known after building it.

### `waveflow.calib.resource_model` — resource_model.py — predicting a design's resource utilization by composing per-module models.

**Classes:**
- `FittedResourceModel` — Counters regressed from physically-motivated features — for LUT and FF.  _(methods: confidence, confidence_feat, declared_counters, fit, has_free_params, predict_feat, transform)_
- `InterfaceResourceModel` — A composite's **own** cost — the adapters, channel FIFOs, control block and DATAFLOW shell.  _(methods: confidence_feat, declared_counters, get_params, load_table, predict_feat, table_from_store)_
- `LookupResourceModel` — Look the measurement up for exactly this configuration; refuse to interpolate.  _(methods: confidence, confidence_feat, counters, declared_counters, fit, get_params, predict, predict_feat)_
- `PriorResourceModel` — Counters computed from a formula over the module's features.  _(methods: confidence, confidence_feat, declared_counters, predict_feat, transform)_
- `ResourceEstimate` — A composed prediction, with the breakdown and the weakest confidence that fed it.  _(methods: level, to_json, weakest)_
- `ResourceModel` — A module's **own** utilization — what it costs beyond its children.  _(methods: check_counters, confidence, corpus, counters, declared_counters, fit, get_params, has_free_params, predict, targets)_

**Functions:**
- `add_counters(*terms: 'dict') -> 'dict'` — Sum counter dicts.  Missing counters are 0; **negatives are preserved** (see the module note).
- `boundary_signature(comp: 'Any') -> 'tuple'` — A canonical descriptor of *comp*'s interface graph: its external ports and internal channels.
- `compose(top: 'Any', model_for=None) -> 'ResourceEstimate'` — Predict *top*'s total area by walking its graph: own model + Σ children, recursively.
- `zero_counters() -> 'dict'`

### `waveflow.calib.synth_report` — synth_report.py — attribute a csynth report's resources to the Waveflow modules that caused them.

**Classes:**
- `ModuleResources` — One Waveflow module's resources, as attributed from the report.  _(methods: to_json)_
- `SynthReport` — A whole-top synthesis run, attributed.  _(methods: integration, module_sum, to_json)_
- `UnmappedModuleError` — A Waveflow module has no matching row in the synthesis report.

**Functions:**
- `attribute_resources(top_comp: 'Any', parser: 'Any', *, top_name: 'str | None' = None) -> 'SynthReport'` — Attribute *parser*'s per-module figures to the modules of the elaborated *top_comp*.
- `report_from_solution(top_comp: 'Any', sol_path: 'str | Path', *, top_name: 'str | None' = None) -> 'SynthReport'` — :func:`attribute_resources` against the ``csynth.xml`` under a Vitis solution directory.
- `rtl_prefix(kernel_task: 'Any') -> 'str'` — The RTL entity-name prefix a :class:`KernelTask` produces.
- `store_report(report: 'SynthReport', store: 'Any', identities: "'dict[str, ModuleIdentity]'", *, source: 'str' = 'hls_estimate', part: 'str' = '', period_ns: 'float' = 0.0, tool: 'str' = '', cost_seconds: 'float' = 0.0, top_identity: "'ModuleIdentity | None'" = None, boundary: "'dict | None'" = None) -> 'list'` — File *report*'s figures into a :class:`~waveflow.calib.record_store.ModuleStore`.

### `waveflow.calib.timing_model` — timing_model.py — per-component timing calibration, orchestration over :mod:`calib`.

**Classes:**
- `StreamTimingModel` — A mem-stream component's residual model: basis ``[nwords, num_trans]`` — the counts the RTL
- `TimingModel` — A component's timing residual model plus the corpus that fits it.  _(methods: collect_pysim, collect_rtl, confidence_feat, data_dir, estimate, fit, gen_data_frame, get_params, is_record_valid, predict_feat, reset, targets)_

### `waveflow.calib.vitis_model` — vitis_model.py — the default resource model for AMD/Xilinx flows.

**Classes:**
- `Counter` — A counter or address register naming one of ``over`` items.  Contributes ``addr_bits``.
- `Crossbar` — Any-lane-to-any-position routing: a variable-position mux, a barrel shifter, a crossbar.
- `DesignStructure` — What a module contains, in the terms the device rules price.  _(methods: basis_terms, basis_terms_from, flatten)_
- `MemArray` — One array as the design partitions it.
- `MultGroup` — ``count`` multipliers with ``operand_bits``-wide signed operands.
- `PerLane` — A datapath replicated once per lane -- adders, comparators, enables, muxes of fixed width.
- `ReductionTree` — A sum/max/and across ``lanes``.  ``lanes - 1`` operators, ``log2(lanes)`` deep.
- `VitisDerived` — The **zero-parameter half**: DSP, block RAM and URAM from the recorded structure.  _(methods: confidence_feat, predict_feat, targets)_
- `VitisResourceModel` — DSP/BRAM from declared structure, LUT/FF from an optional fit.  _(methods: basis_for, confidence, confidence_feat, corpus, declared_counters, derived, fit, fit_features, get_params, has_free_params, load_or_fit, memory_bindings, predict, predict_feat, save_model, structure, sub_models, transform, uncovered)_

### `waveflow.hw.arrayutils` — Generate Vitis HLS array read and write helpers for packed element arrays.

**Classes:**
- `ArrayUtilsStep` — Build step that generates packed-array helper headers for one element type.  _(methods: build_outputs, generate, resolve_deps)_

**Functions:**
- `array(elem_type: 'type[T]', data, static: 'bool' = False) -> 'DataArray'` — Construct a :class:`~waveflow.hw.dataschema.DataArray` instance wrapping *data*.
- `gen_array_utils(elem_type: 'type[DataSchema]', word_bw_supported: 'list[int]', cfg: 'BuildConfig | None' = None, streamutils_dir: 'Path | str | None' = None) -> 'Path'` — Generate a Vitis HLS header that reads and writes packed arrays of one element type.
- `get_nwords(elem_type: 'type[DataSchema]', word_bw: 'int', shape: 'int | tuple[int, ...] | list[int]') -> 'int'` — Return the packed word count for an array shape at a given word width.
- `read_array(packed: 'Words', elem_type: 'type[T]', word_bw: 'int', shape: 'int | tuple[int, ...] | list[int]') -> 'DataArray'` — Unpack hardware words into a :class:`~waveflow.hw.dataschema.DataArray` instance.
- `read_uint32_file(file_path: 'str | Path', elem_type: 'type[DataSchema]', shape: 'int | tuple[int, ...] | list[int]') -> 'Any'` — Read packed 32-bit words from a binary file into a Python array.
- `write_array(arr: 'DataArray | Any', elem_type: 'type[T] | None' = None, *, word_bw: 'int') -> 'Words'` — Pack a Python array of schema elements into hardware words.
- `write_uint32_file(arr: 'Any', elem_type: 'type[DataSchema]', file_path: 'str | Path', write_slice: 'Any' = None, nwrite: 'int | None' = None) -> 'Path'` — Pack an array into 32-bit words and write it to a binary file.

### `waveflow.hw.aximm_queue` — aximm_queue.py — memory-backed AXI-MM ring buffer (SPSC FIFO).

**Classes:**
- `AXIMMQueue` — Proxy a master uses to ``write``/``get`` a memory-backed ring buffer.  _(methods: count, get, reset, space, try_get, try_write, write)_
- `AXIMMQueueGetStmt` — IR node produced by ``AXIMMQueue.get(schema_type)`` calls — a memory-backed
- `AXIMMQueueLayout` — Memory map for a ring buffer in an AXI-MM region.  _(methods: capacity_addr, control_bytes, data_base, head_addr, slot_addr, tail_addr, total_bytes, word_bytes)_

### `waveflow.hw.clock`

**Classes:**
- `Clock` — Represents a clock domain in the hardware design.  _(methods: period)_

### `waveflow.hw.complexfield` — ``ComplexField`` -- a complex DataSchema type generic over a scalar inner field.

**Classes:**
- `ComplexField` — Complex element type generic over a scalar inner field (real, imag share a format).  _(methods: from_uint_expr, get_codegen_includes, init_value, inner_format, specialize, to_uint_expr, to_uint_value_expr)_

**Functions:**
- `cadd(a: 'DataArray', b: 'DataArray') -> 'DataArray'` — ``a + b`` componentwise (full precision; int bits +1).
- `cmult(a: 'DataArray', b: 'DataArray') -> 'DataArray'` — ``a * b`` -- complex multiply; result ``(2W+1, 2I+1, signed)`` (signed inner only).
- `conj(a: 'DataArray') -> 'DataArray'` — ``conj(a)`` -- imag negated; result ``(W+1, I+1, signed)`` (signed inner only).
- `csub(a: 'DataArray', b: 'DataArray') -> 'DataArray'` — ``a - b`` componentwise (full precision; always signed).
- `csum(a: 'DataArray', axis: 'int' = 0) -> 'DataArray'` — Wide-accumulator column reduction (full precision), real **or** complex.

### `waveflow.hw.dataschema` — Class-driven schema architecture.

**Classes:**
- `BooleanField` — A 1-bit boolean flag field.  _(methods: init_value, specialize)_
- `DataArray` — Class-driven array schema with fixed maximum shape and optional dynamic extent.  _(methods: can_pack_whole, gen_class_elems, gen_dump_json, gen_load_json, gen_nwords_len_helpers, gen_pack, gen_stream_helpers, gen_unpack, get_bitwidth, get_dependencies, get_param_str, get_utility_includes, init_value, is_close, nwords_per_inst, specialize, val, validate_specialize_kwargs, write_uint32_file)_
- `DataField` — Base class for scalar fields.  _(methods: cpp_class_name, from_uint_expr, from_words_numpy, gen_pack, gen_unpack, get_bitwidth, is_close, to_uint_expr, to_uint_value_expr, to_words_numpy, val)_
- `DataList` — Structured aggregate with schema defined entirely at the class level.  _(methods: gen_dump_json, gen_load_json, gen_pack, gen_unpack, get_bitwidth, get_bitwidth_active, get_dependencies, get_element_definition, get_element_description, get_element_schema, get_utility_includes, init_value, is_close, nwords_per_inst, val)_
- `DataSchema` — Abstract base class for schema nodes.  _(methods: as_buildable, cpp_class_name, default_include_filename, default_tb_include_filename, deserialize, from_dict, from_json, from_uint_expr, from_words_numpy, gen_pack, gen_read, gen_unpack, gen_write, get_bitwidth, get_bitwidth_active, get_bitwidth_max, get_codegen_includes, get_dependencies, get_param_str, get_utility_includes, include_guard, include_path, init_value, is_close, merge_specialize_attrs, nwords_active, nwords_max, nwords_per_inst, read_uint32_file, relative_include_path_to, relative_tb_include_path_to, resolved_include_filename, resolved_tb_include_filename, serialize, tb_include_guard, tb_include_path, tb_member_macro, to_dict, to_json, to_uint_expr, to_uint_value_expr, to_words_numpy, validate_specialize_kwargs, write_uint32_file)_
- `DataSchemaStep` — Buildable that generates C++ include headers for a :class:`DataSchema` class.  _(methods: build_outputs, generate, include_guard, include_path, resolve_deps, tb_include_guard, tb_include_path)_
- `EnumField` — Enum scalar field specialized by IntEnum type and optional bitwidth/default.  _(methods: from_uint_expr, get_dependencies, init_value, is_close, specialize, to_uint_expr, to_uint_value_expr)_
- `FloatField` — Floating-point scalar field specialized by bitwidth.  _(methods: from_uint_expr, init_value, is_close, specialize, to_uint_expr, to_uint_value_expr)_
- `IntField` — Integer scalar field for arbitrary bitwidth and signedness.  _(methods: init_value, specialize, to_uint_expr, to_uint_value_expr)_
- `MemAddr` — Unsigned address field specialized by bitwidth.  _(methods: specialize)_
- `ParamSchema` — A :class:`DataList` whose ``elements`` reference symbolic :class:`Param`\ s — the  _(methods: specialize)_
- `VarDataArray` — Variable-length array schema with an in-band length field.  _(methods: cpp_class_name, from_dict, get_bitwidth, get_bitwidth_active, get_dependencies, init_value, is_close, nwords_per_inst, specialize, to_dict, val)_

### `waveflow.hw.dataunion` — DataUnion: schema registry, schema-ID field, length field, and variable-schema burst.

**Classes:**
- `DataUnion` — Variable-schema burst: a fixed header combined with one of N registered payload types.  _(methods: cpp_class_name, deserialize, gen_include, hdr, include_path, max_payload_bw, max_payload_nwords, nwords_per_inst, payload, schema_id, serialize, specialize)_
- `DataUnionHdr` — DataList header carrying a schema_id and a payload word count (nwords).  _(methods: specialize)_
- `LengthField` — Unsigned integer field for word counts or byte lengths.  _(methods: specialize)_
- `SchemaIDField` — Unsigned integer field whose valid values are restricted to a SchemaRegistry.  _(methods: specialize)_
- `SchemaRegistry` — Bidirectional mapping between integer IDs and DataSchema subclasses.  _(methods: contains_class, contains_id, get_class, get_id, items, next_id, register, registered_ids)_

**Functions:**
- `register_schema(schema_id: 'int | None' = None, *, registry: 'SchemaRegistry')` — Class decorator: register a DataSchema subclass in *registry*.

### `waveflow.hw.fixpoint` — Integer-backed, Vitis-bit-exact fixed-point DataSchema type + vector arithmetic.

**Classes:**
- `FixedField` — Fixed-point element type, bit-exact with ``ap_fixed<W, I, Q, O>``.  _(methods: from_uint_expr, get_format, init_value, real, specialize, to_uint_expr, to_uint_value_expr)_

**Functions:**
- `add(a: 'DataArray', b: 'DataArray') -> 'DataArray'`
- `fixed_sum(a: 'DataArray', axis: 'int | None' = None) -> 'DataArray'` — Full-precision reduction (integer bits grow by ``ceil(log2 N)``).
- `from_real(values: 'Any', fixed_cls: 'type[FixedField]') -> 'DataArray'` — Quantize real value(s) into ``DataArray[fixed_cls]`` (the load-data path).
- `mult(a: 'DataArray', b: 'DataArray') -> 'DataArray'`
- `quantize(a: 'DataArray', target: 'type[FixedField]') -> 'DataArray'` — Requantize to a declared ``target`` format — the only lossy operation.
- `shift(a: 'DataArray', n: 'int') -> 'DataArray'` — Lossless rescale by ``2^n`` (point-move): stored bits unchanged.
- `sub(a: 'DataArray', b: 'DataArray') -> 'DataArray'`
- `to_real(da: 'DataArray') -> 'np.ndarray'` — The real-valued view of a ``DataArray[FixedField]``.

### `waveflow.hw.hw_freerun` — hw_freerun.py — :class:`FreeRunMod`, the free-running (``ap_ctrl_none``) synthesizable component.

**Classes:**
- `FreeRunMod` — A free-running synthesizable component — a leaf (implements :meth:`run_iter`) **or** a composite  _(methods: add_timing_model, boundary, internal_edges, kernel_task, ordered_subcomps, run_iter, run_proc, timed_delay, timing_model)_

**Functions:**
- `discover_timing_models(root) -> "list[tuple['FreeRunMod', object]]"` — Walk *root* and its ``sub_comps`` for every attached ``TimingModel``.

### `waveflow.hw.hw_hostactivated` — hw_hostactivated.py — :class:`HostActivated`, the host-activated (regmap-launched) synth leaf.

**Classes:**
- `HostActivated` — A host-activated (regmap-launched) synthesizable leaf: implement :meth:`on_start`.  _(methods: on_start, run_once, run_once_sim)_

### `waveflow.hw.hw_module`

**Classes:**
- `ControlMode` — Create a collection of name/value pairs.
- `DynParam` — Marks a field as an **init-time** parameter — a knob on a fixed artifact.
- `HwConst` — Marks a class attribute as a class-level constant.
- `HwModule` — Base class for synthesizable hardware components.  _(methods: add_comp, add_endpoint, add_if, add_rm, add_rm_self, add_state, get_rm, resource_model, resource_structure)_
- `HwParam` — Marks a dataclass field as a C++ template parameter.
- `HwParamValue` — Int subclass that remembers which ``HwParam`` field it was bound to.
- `SynthContext` — Parameter context passed to every ``synth_fn`` during codegen.  _(methods: cpp_param, from_component)_

**Functions:**
- `discover_dyn_params(obj: 'Any') -> 'dict[str, Any]'` — Return ``{field: value}`` for every ``DynParam`` field of *obj* whose value differs from the
- `discover_hw_const(cls) -> 'dict[str, Any]'` — Walk the MRO and return ``{field_name: value}`` for every ``HwConst`` field.
- `discover_state(obj: 'Any') -> "dict[str, 'HwState']"` — Return ``{name: HwState}`` for every object registered via :meth:`HwModule.add_state`, in
- `state_entry_for(comp: 'Any', obj: 'Any') -> "'HwState | None'"` — Return the registered :class:`~waveflow.hw.hw_state.HwState` that **is** *obj*, or ``None``.
- `validate_param_supports(comp_class) -> 'None'` — Validate ``comp_class.param_supports``; raise ``SynthesisError`` on any violation.

### `waveflow.hw.hw_state` — hw_state.py — :class:`HwState`, storage that lives **inside** a hardware module.

**Classes:**
- `HwState` — A named piece of storage inside a :class:`~waveflow.hw.hw_module.HwModule`.  _(methods: schema, val)_

**Functions:**
- `validate_bind_storage(spec: 'Any', who: 'str' = 'HwState') -> 'None'` — Reject a malformed ``bind_storage`` spec at construction time.
- `validate_partition(spec: 'Any', who: 'str' = 'HwState') -> 'None'` — Reject a malformed ``partition`` spec at construction time, not at csynth.

### `waveflow.hw.hw_testbench` — ``SeqTB`` — a sequential codegen-source testbench (a program that drives a kernel).

**Classes:**
- `HwTestbench` — Base class for codegen-source testbenches.  _(methods: main, now, run, timeout, transaction_seconds)_
- `SeqTB` — Base class for codegen-source testbenches.  _(methods: main, now, run, timeout, transaction_seconds)_

### `waveflow.hw.hwstmt` — HwStmt — synthesizable statement IR.

**Classes:**
- `CaseStmt` — Restricted ``if var.field <op> value:`` — maps to switch/if-else in C++.
- `ContinueStmt` — ``continue`` inside ``while True``.
- `DutBindStmt` — A ``dut = <HwComponentSubclass>(**kwargs)`` binding inside ``main()``.
- `FieldRef` — Field access on a bound ``HwVar`` (e.g. ``cmd_hdr.nsamp``).
- `FunctionStmt` — Call to a user-written ``@synthesizable`` method (no ``synth_fn``).
- `HwExpr` — Base class for synthesizable expression nodes.
- `HwStmt` — Base class for synthesizable statement IR nodes.
- `HwVar` — A symbolic variable produced by a synthesizable statement.
- `KernelCallStmt` — A ``<dut>.run()`` call site: invoke the DUT's kernel function.
- `MMArrayReadStmt` — IR node for ``port.read_array(elem_type, count, addr)`` — an m_axi  _(methods: addr_expr, count_expr, elem_type, max_expr, port, target_var)_
- `MMArrayWriteStmt` — IR node for ``port.write_array(buf, elem_type, addr, count)`` — an m_axi  _(methods: addr_expr, count_expr, elem_type, max_expr, port, source_expr)_
- `MemAllocArrayStmt` — A ``<cmd>.<field> = <mem>.alloc_array(<buf>, ElemT, count=<expr>)`` call.
- `MemBindStmt` — A ``mem = MemoryMod(**kwargs)`` binding inside a TB ``main()``.
- `MemReadArrayStmt` — A ``<out> = <mem>.read_array(<addr_expr>, ElemT, count=<expr>)`` call.
- `Ref` — A reference to a bound ``HwVar``.
- `ReturnStmt` — ``return`` from the kernel function. Optional return value.
- `SchemaBindStmt` — A ``name = <DataSchemaSubclass>()`` binding inside ``main()``.
- `SeqStmt` — Sequential list of statements.
- `SynthCallStmt` — A call to a ``@synthesizable`` method with a ``synth_fn``.
- `TbCallStmt` — A testbench-mode method call (push, pop, read_uint32_file, ...).
- `TbFileIOStmt` — File I/O on a TB schema local: ``read_uint32_file*`` /
- `TbRegmapFileReadStmt` — Regmap file preload helper in testbench mode.
- `TbStatusJsonStmt` — ``dut.regmap.write_status_json(path, fields=[...])`` — write the
- `TbStreamIOStmt` — Stream IO on a DUT endpoint: ``dut.<ep>.push/pop[_array](...)``.
- `WhileStmt` — ``while True:`` loop — maps to ``ap_ctrl_none`` in HLS.

### `waveflow.hw.interface` — interface.py: Hardware interfaces and endpoints (graph + simulation hooks).

**Classes:**
- `CapabilityView` — A direction-restricted proxy over an :class:`InterfaceEndpoint`.  _(methods: direction, endpoint)_
- `CrossBarIF` — A crossbar interface connecting ``nports_in`` input ports to ``nports_out``  _(methods: bind, write)_
- `CrossBarIFInput` — An input endpoint for a :class:`CrossBarIF`.
- `CrossBarIFOutput` — An output endpoint for a :class:`CrossBarIF`.
- `Interface` — Base class for an interface with a set of named sides.  _(methods: bind)_
- `InterfaceEndpoint` — Base class for a concrete endpoint owned by a component.  _(methods: as_dir, bind)_
- `QueuedTransferIF` — Base class for interfaces that transfer queued word bursts with optional latency.
- `QueuedTransferIFMaster` — Base class for master/input endpoints that push word bursts into an interface.  _(methods: write)_
- `QueuedTransferIFSlave` — Base class for slave/output endpoints that buffer incoming word bursts.  _(methods: get, run_proc)_
- `SobIFMaster` — Producer (``write_lock``) side of a :class:`StreamOfBlocksIF`.  _(methods: acquire_write, bitwidth, commit_write)_
- `SobIFSlave` — Consumer (``read_lock``) side of a :class:`StreamOfBlocksIF`.  _(methods: acquire_read, bitwidth, release_read)_
- `StreamDrainStmt` — IR node produced by ``StreamIFSlave.drain()`` calls.
- `StreamGetStmt` — IR node produced by ``StreamIFSlave.get(...)`` calls.
- `StreamIF` — A stream interface with 'master' (TX) and 'slave' (RX) sides.  _(methods: bind, write)_
- `StreamIFMaster` — A stream master (TX) endpoint that provides a write function.  _(methods: push, push_array, write, write_pipelined)_
- `StreamIFSlave` — A stream slave (RX) endpoint that is realized as a function call.  _(methods: drain, get, get_pipelined, pop, pop_array)_
- `StreamOfBlocksIF` — The block interface (``SOBIF``): a depth-2 ping-pong buffer between one  _(methods: acquire_read, acquire_write, bind, commit_write, release_read)_
- `StreamWriteStmt` — IR node produced by ``StreamIFMaster.write(...)`` calls.

**Functions:**
- `port_read(fn)` — Tag a transaction method as exercising the **read** channel (``'R'``).
- `port_write(fn)` — Tag a transaction method as exercising the **write** channel (``'W'``).

### `waveflow.hw.mem_stream` — mem_stream.py — the two reusable memory-endpoint components: ``MemRStream`` / ``MemWStream``.

**Classes:**
- `KernelTask` — The fixed ``hls::task`` body descriptor a composable component exposes for the composite
- `MRCmd` — One ``MemRStream`` command (host/sequencer -> ``s_cmd``): burst ``len`` packed words
- `MWCmd` — One ``MemWStream`` command (host/sequencer -> ``s_cmd``): drain ``len`` words off ``s_in``
- `MemComplete` — The completion echo a ``MemWStream``/``MemRStream`` with ``emit_done=True`` writes on
- `MemRCmd` — In-band ``MemRStream`` descriptor: relay ``fwd_bursts`` opaque bursts, then burst ``len`` words
- `MemRStream` — The sole ``m_axi`` **read** owner: an :class:`MRCmd` queue -> word-granular ``m_out`` burst.  _(methods: Cmd, bind_base, kernel_task, run_iter)_
- `MemWCmd` — In-band ``MemWStream`` descriptor: buffer the next ``fwd_bursts`` opaque bursts, drain ``len``
- `MemWStream` — The mirror of :class:`MemRStream`: the sole ``m_axi`` **write** owner.  An :class:`MWCmd`  _(methods: Cmd, bind_base, kernel_task, run_iter)_

### `waveflow.hw.memif` — memif.py: Memory-mapped (MM) interface endpoints and interconnects.

**Classes:**
- `AXIMMAddressRange` — Half-open byte-address range [base_addr, base_addr + size).  _(methods: contains, to_local)_
- `AXIMMCrossBarIF` — AXI-MM crossbar connecting ``nports_master`` master ports to  _(methods: bind, read, read_spanned, write, write_spanned)_
- `AXIMMCrossBarIFMaster` — Master (initiator) endpoint for any MM interconnect.  _(methods: poll_until, read, read_array, read_array_anchored, read_array_pipelined, read_schema, read_spanned, region, write, write_array, write_array_pipelined, write_schema, write_spanned)_
- `AXIMMCrossBarIFSlave` — Slave (target) endpoint for any MM interconnect.
- `AXIMMProtocol` — AXI-MM protocol variant for a crossbar slave port.
- `BusTiming` — Per-direction bus-transfer span model owned by an :class:`MMIFSlave` (the  _(methods: read_span_secs, write_span_secs)_
- `DirectMMIF` — Simple point-to-point MM interconnect: one master, one slave.  _(methods: bind, read, write)_
- `Eq` — Poll predicate ``read(addr) == rhs``.
- `LoweredPollCond` — The lowered, synthesizable form of a :class:`PollCond` — what the
- `MMIFMaster` — Master (initiator) endpoint for any MM interconnect.  _(methods: poll_until, read, read_array, read_array_anchored, read_array_pipelined, read_schema, read_spanned, region, write, write_array, write_array_pipelined, write_schema, write_spanned)_
- `MMIFReadMaster` — An ``m_axi`` master this component only **reads** through.
- `MMIFSlave` — Slave (target) endpoint for any MM interconnect.
- `MMIFWriteMaster` — An ``m_axi`` master this component only **writes** through.
- `Ne` — Poll predicate ``read(addr) != rhs`` (e.g. the ring dequeue ``tail != head``).
- `PollCond` — A small declarative poll predicate usable by both sim and (later) synth.  _(methods: eval)_
- `PollUntilStmt` — IR node for ``val = master.poll_until(addr, cond, poll_interval)`` — a  _(methods: addr_expr, cond, port)_
- `Region` — Element-indexed view of a memory region for an :class:`MMIFMaster` (built via  _(methods: as_dir, byte_of, read_slice, read_slice_pipelined, write_slice, write_slice_pipelined)_

**Functions:**
- `assign_address_ranges(slaves: 'list[MMIFSlave]', ranges: 'list[tuple[int, int]]') -> 'list[AXIMMAddressRange]'` — Assign address ranges to slave endpoints before simulation starts.

### `waveflow.hw.memory`

**Classes:**
- `AddrUnit` — Create a collection of name/value pairs.
- `MemMgr` — Allocation policy and address arithmetic — **the manager, not the memory**.  _(methods: addr_to_index, index_to_addr, place, word_nbytes)_
- `MemSeg` — One memory region tied to a burst bundle — mirrors the C++ ``wfbfm::MemSeg``.  _(methods: to_cpp)_
- `Memory` — Sparse memory model for Waveflow.  _(methods: alloc, free, read, write)_
- `MemoryMod` — A latency-modeling :class:`~waveflow.simulation.simobj.SimObj` that wraps a  _(methods: alloc, alloc_array, bfm_model, free, pre_sim, read_array)_

### `waveflow.hw.param` — Symbolic parameters for declarative schema specialization (Level-2 parameterization).

**Classes:**
- `Expr` — A symbolic arithmetic node: ``op(left, right)`` over params / values.  _(methods: resolve)_
- `LazyField` — A deferred ``specialize`` call: records ``(fn, args, kwargs)``; :meth:`resolve`  _(methods: resolve)_
- `Param` — A named symbolic parameter with a default value.  _(methods: resolve)_
- `Symbolic` — Base for symbolic values: :class:`Param` leaves and :class:`Expr` nodes.  _(methods: resolve)_

**Functions:**
- `defer_if_symbolic(fn: 'Callable[..., Any]') -> 'Callable[..., Any]'` — Wrap a ``specialize`` so it returns a :class:`LazyField` when any argument is
- `is_symbolic(x: 'Any') -> 'bool'` — Whether ``x`` (or, recursively, any element of a tuple/list) is symbolic or lazy.
- `pmax(a: 'Any', b: 'Any') -> 'Expr'` — Symbolic ``max`` (e.g. aligning fractional widths).
- `pmin(a: 'Any', b: 'Any') -> 'Expr'` — Symbolic ``min``.
- `resolve_elements(elements: 'dict[str, Any]', env: 'dict[str, Any]') -> 'dict[str, Any]'` — Resolve a schema ``elements`` dict: any ``LazyField`` value (or ``schema`` inside a

### `waveflow.hw.regmap` — regmap.py — Register-map abstraction for Waveflow.

**Classes:**
- `BoundRegMap` — Host-side proxy binding a :class:`RegMap` to an :class:`MMIFMaster`  _(methods: base_addr, get, poll_end, set, start)_
- `RegAccess` — Host access mode for a register field.
- `RegField` — Declaration of one register map field.
- `RegMap` — Ordered collection of RegFields with per-field numpy word buffers.  _(methods: bind_master, bit_fields_at_offset, bit_offset_of, field_name_at_offset, get, nwords_of, offset_of, read_uint32_file, read_word, set, total_size_bytes, write_status_json, write_word)_
- `RegMapAccessError` — Raised on host access-mode violation, offset miss, or unaligned address.
- `RegMapGetStmt` — Synthesizable read of a regmap field — emits an AXI-Lite scalar read.
- `RegMapMMIFSlave` — MMIFSlave subclass that dispatches reads/writes to a RegMap.
- `RegMapSetStmt` — Synthesizable write to a regmap field — emits an AXI-Lite scalar write.
- `VitisRegMap` — RegMap mirroring the s_axilite control layout Vitis HLS generates.  _(methods: start)_
- `VitisRegMapMMIFSlave` — RegMapMMIFSlave that invokes an ``on_start`` generator on ap_start writes.

### `waveflow.hw.schema_transfer_interface` — Logical interfaces for transmitting serializable objects over physical transports.

**Classes:**
- `ArrayTransferIF` — Logical interface container for ArrayTransferIFMaster / ArrayTransferIFSlave.  _(methods: bind)_
- `ArrayTransferIFMaster` — Master endpoint: serializes an iterable of *element_type* instances and  _(methods: write)_
- `ArrayTransferIFSlave` — Slave endpoint: receives a word burst and deserializes it into a list of  _(methods: get, pre_sim)_
- `PhysicalTransport` — Abstract physical transport: send a word burst and register a receive callback.  _(methods: get_words, set_rx_callback, write_words)_
- `SchemaTransferIF` — Logical interface container for SchemaTransferIFMaster / SchemaTransferIFSlave.  _(methods: bind)_
- `SchemaTransferIFMaster` — Master endpoint: serializes any object and forwards words to the transport.  _(methods: write)_
- `SchemaTransferIFSlave` — Slave endpoint: receives word bursts and deserializes them via *schema_type*.  _(methods: get, pre_sim)_
- `StreamTransport` — Physical transport backed by StreamIFMaster / StreamIFSlave.  _(methods: get_words, set_rx_callback, write_words)_

### `waveflow.hw.synth`

**Functions:**
- `sim_only(fn)` — Mark a method as simulation-only — skipped silently during synthesis.
- `synthesizable(fn=None, *, synth_fn=None, stmt_class=None, impl_file=None)` — Mark a method as synthesizable to HLS C++.

### `waveflow.mcp.build_corpus` — CLI entry point: build_corpus

**Functions:**
- `build_corpus(*, verbose: 'bool' = True) -> 'Path'` — Build the corpus directory and return its path.
- `main() -> 'None'` — Console-script entry point for ``build_corpus``.

### `waveflow.mcp.cli_build_example_rag` — CLI entrypoint: build or refresh the OpenAI vector store for waveflow examples.

**Functions:**
- `build_example_rag(*, verbose: 'bool' = True) -> 'str'` — Build (or rebuild) the OpenAI vector store for waveflow examples.
- `decode_upload_filename(filename: 'str') -> 'str'` — Decode an uploaded filename back to a corpus-relative path.
- `encode_upload_filename(path: 'str') -> 'str'` — Encode a corpus-relative path into a filename safe for OpenAI upload.
- `main() -> 'None'` — Console-script entry point for ``waveflow-build-example-rag``.

### `waveflow.mcp.components` — Deterministic glossary of waveflow schema vocabulary.

**Functions:**
- `get_components() -> 'dict'` — Return the canonical waveflow schema vocabulary glossary.

### `waveflow.mcp.example_rag` — OpenAI vector-store backed search over the packaged waveflow examples corpus.

**Functions:**
- `search_schema_examples(task: 'str', keywords: 'list[str] | None' = None, k: 'int | None' = 5) -> 'dict[str, Any]'` — Search the OpenAI-hosted vector store for schema examples.

### `waveflow.mcp.file_tools` — Generic file tools for headless mode.

**Functions:**
- `make_file_tools(work_dir: 'str | os.PathLike[str]') -> 'tuple[Any, Any, Any, Any]'` — Return ``(list_files, read_file, write_file, edit_file)`` scoped to *work_dir*.

### `waveflow.mcp.headless` — Headless LLM harness for waveflow MCP tools.

**Functions:**
- `main() -> 'None'`
- `run_session(*, task: 'str', model: 'str' = 'gpt-4.1', max_rounds: 'int' = 10, verbose: 'bool' = False, output_path: 'str | os.PathLike[str] | None' = None, mode: 'str | None' = None, work_dir: 'str | os.PathLike[str] | None' = None) -> 'dict[str, Any]'` — Run a simulated headless session for *task*.

### `waveflow.mcp.registry` — Shared MCP tool registry for waveflow.

**Classes:**
- `ToolRegistry` — Registry of waveflow MCP tools.  _(methods: add, dispatch, register_all, tool_schemas)_

### `waveflow.mcp.schema_examples` — Schema example registry for waveflow MCP tools.

**Functions:**
- `get_schema_example(example_id: 'str') -> 'dict'` — Return the full content for a schema example by ID.
- `list_schema_examples() -> 'dict'` — Return a compact catalog of available schema examples.

### `waveflow.mcp.schema_tools` — Deterministic schema-planning and validation helpers for waveflow MCP tools.

**Functions:**
- `get_schema_draft_plan(task: 'str | None' = None, workspace_root: 'str | None' = None) -> 'dict'` — Return a deterministic workflow for drafting a new schema.
- `validate_schema(schema: 'str', workspace_root: 'str | None' = None) -> 'dict'` — Validate a drafted waveflow schema provided as Python source text.
- `validate_schema_from_file(schema_name: 'str', input_path: 'str', output_path: 'str') -> 'dict[str, Any]'` — Validate a waveflow schema file and write a report artifact.

### `waveflow.mcp.server`

**Functions:**
- `build_mcp(mode: 'str' = 'workspace', work_dir: 'str | os.PathLike[str] | None' = None) -> 'FastMCP'` — Create and configure a waveflow MCP server for the given *mode*.
- `main() -> 'None'`

### `waveflow.named`

**Classes:**
- `NamedObject` — Base class for objects that have a type name and instance name.

### `waveflow.scripts.build_lab_autograder`

**Functions:**
- `load_config(config_path)`
- `main()`

### `waveflow.scripts.clean_examples` — Remove regenerable Vitis/Vivado HLS build artifacts under examples/.

**Functions:**
- `dir_size_bytes(path: pathlib.Path) -> int`
- `find_artifact_dirs(examples_root: pathlib.Path) -> list[pathlib.Path]` — Top-level artifact dirs (don't descend into an already-matched dir).
- `find_repo_root(start: pathlib.Path) -> pathlib.Path` — Walk upward from `start` until a directory containing pyproject.toml is found.
- `main() -> None`

### `waveflow.scripts.collect_overlay`

**Functions:**
- `main()`

### `waveflow.scripts.create_doc_assets` — create_doc_assets.py - Incremental doc asset (figure) generator for waveflow.

**Classes:**
- `DocSet` — Describes a single doc-figure set and knows how to build itself.  _(methods: build, is_stale, missing_outputs, newest_dep_mtime, oldest_output_mtime, status_lines)_

**Functions:**
- `main(argv: 'list[str] | None' = None) -> 'int'`
- `run(doc_names: 'list[str]', *, status_only: 'bool' = False, force: 'bool' = False) -> 'int'` — Process the requested doc sets.

### `waveflow.scripts.sv_build`

**Functions:**
- `ensure_dir(path: pathlib.Path)`
- `run(cmd, cwd=None)`
- `run_impl()`
- `run_synth()`

### `waveflow.scripts.sv_sim`

**Functions:**
- `main()`
- `run(cmd, cwd=None)`

### `waveflow.scripts.waveflow_mcp_setup`

**Functions:**
- `build_parser() -> argparse.ArgumentParser`
- `load_template() -> dict`
- `main() -> int`
- `render_mcp_config(*, python_path: str, vector_store_id: str | None = None) -> str`
- `validate_python_interpreter(python_path: str) -> None`
- `write_mcp_config(*, workspace: pathlib.Path, python_path: str, force: bool = False, vector_store_id: str | None = None) -> pathlib.Path`

### `waveflow.scripts.xsim_vcd` — xsim_vcd.py — Run Vivado simulation with VCD output

**Functions:**
- `copy_vcd(sim_dir, base_dir, component_path, output_vcd)`
- `create_vcd_batch(top_name, original_bat, new_bat)`
- `main()`
- `modify_tcl(tcl_path, tcl_vcd_path, trace_level)`
- `parse_args()`
- `run_batch(batch_path)`
- `run_xsim_vcd(top: str, comp: str = 'hls_component', out: str = 'dump.vcd', soln: str | None = 'solution1', trace_level: str = '*', workdir: str | pathlib.Path | None = None) -> pathlib.Path` — Generate a VCD file by re-running a Vivado HLS RTL simulation.

### `waveflow.simulation.logger` — Simulation event logger.

**Classes:**
- `Logger` — Timestamped CSV event logger for SimPy simulations.  _(methods: error_cleanup, get_tv, log, post_sim)_
- `NullLogger` — Drop-in replacement for Logger that discards all entries.  _(methods: log)_

### `waveflow.simulation.simobj`

**Classes:**
- `ActionOverlap` — Represents an overlap between two actions on the same object.
- `ActionRecord` — Timing record for one action invocation.
- `SimObj` — Base class for simulation entities built on top of ``simpy``.  _(methods: action, active_overlap_count, add_process, clear_action_logs, container, env, error_cleanup, event, now, post_sim, pre_sim, process, resource, run_proc, start_registered_processes, timeout, transaction_queue)_

### `waveflow.simulation.simulation`

**Classes:**
- `Simulation` — Runtime coordination object for a SimPy-based simulation.  _(methods: add_obj, as_current, run_sim)_

**Functions:**
- `current_simulation() -> "'Simulation | None'"` — Return the ambient :class:`Simulation` set by :meth:`Simulation.as_current`, or ``None``.

### `waveflow.simulation.stream_tb` — stream_tb.py — reusable pysim testbench participants: a stream driver and a stream sink.

**Classes:**
- `StreamDriver` — Plays a burst bundle onto a stream — a schema-blind, file-driven source.  _(methods: bfm_model, pre_sim, run_proc)_
- `StreamSink` — Collects raw word bursts off a stream — a schema-blind sink.  _(methods: bfm_model, rx_proc)_

### `waveflow.toolchain.stagetest` — Helpers for ordered multi-stage test and build flows.

**Classes:**
- `StageTest` — Ordered stage registry for staged demo and test flows.  _(methods: add_stage, from_names, has_stage, range_names, run, stage_index, stage_names, stage_range, stages, validate_range)_
- `TestStage` — Definition of a single named stage in an ordered flow.

### `waveflow.toolchain.toolchain`

**Functions:**
- `find_vitis_path(top_dir: Union[str, pathlib.Path, NoneType] = None) -> Optional[str]` — Locates the Vitis execution entry point for hardware synthesis and simulation.
- `run_vitis_hls(tcl_script: Union[str, pathlib.Path], work_dir: Union[str, pathlib.Path, NoneType] = None, args: Optional[List[str]] = None, capture_output: bool = True, env: Optional[Dict[str, str]] = None) -> subprocess.CompletedProcess` — Execute a Vitis HLS TCL script using the discovered Vitis launcher.
- `run_vitis_hls_result(tcl_script: Union[str, pathlib.Path], work_dir: Union[str, pathlib.Path, NoneType] = None, args: Optional[List[str]] = None, capture_output: bool = True, output_path: Union[str, pathlib.Path, NoneType] = None, env: Optional[Dict[str, str]] = None) -> Dict[str, Optional[str]]` — Execute a Vitis HLS TCL script and return a structured result dictionary.
- `subprocess_result(cmd_list: Sequence[Union[str, pathlib.Path]], work_dir: Union[str, pathlib.Path, NoneType] = None, capture_output: bool = True, output_path: Union[str, pathlib.Path, NoneType] = None, env: Optional[Dict[str, str]] = None) -> Dict[str, Optional[str]]` — Execute a subprocess command and return a structured result dictionary.

### `waveflow.utils.burst_io` — burst_io.py — file-based stream test vectors as a **burst bundle** (a folder).

**Functions:**
- `read_burst_bundle(bundle_dir: 'str | Path') -> 'list[np.ndarray]'` — Read a burst bundle back into a list of word bursts.
- `write_burst_bundle(word_arrays: 'list', bundle_dir: 'str | Path') -> 'Path'` — Write a list of word bursts to a bundle directory.

### `waveflow.utils.complexutils` — Vectorized complex value core over float / fixed / int inners.

**Functions:**
- `cadd(va: 'NDArray', a: 'Format', vb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'` — ``(ar+br) + j(ai+bi)`` -- component ``add`` (full precision, int bits +1).
- `cadd_float(a: 'NDArray', b: 'NDArray') -> 'NDArray'`
- `cadd_format(a: 'Format', b: 'Format') -> 'Format'`
- `cmult(va: 'NDArray', a: 'Format', vb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'` — ``(ar*br - ai*bi) + j(ar*bi + ai*br)`` -- four component ``mult`` + an ``add``/``sub``.
- `cmult_float(a: 'NDArray', b: 'NDArray') -> 'NDArray'`
- `cmult_format(a: 'Format', b: 'Format') -> 'Format'` — ``(ar*br - ai*bi)`` / ``(ar*bi + ai*br)`` -> ``sub_format(P, P)`` over ``P = a*b``.
- `complex_dtype(fmt: 'Format') -> 'np.dtype'` — The structured stored-int dtype ``[('re', D), ('im', D)]`` for a fixed/int inner.
- `conj(va: 'NDArray', a: 'Format') -> 'tuple[NDArray, Format]'` — ``ar - j(ai)`` -- imag negated; result ``(W+1, I+1, signed)`` (lossless re widen).
- `conj_float(a: 'NDArray') -> 'NDArray'`
- `conj_format(a: 'Format') -> 'Format'` — imag is negated -> grow by an int bit and force signed: ``(W+1, I+1, signed)``.
- `csub(va: 'NDArray', a: 'Format', vb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'` — ``(ar-br) + j(ai-bi)`` -- component ``sub`` (full precision, always signed).
- `csub_float(a: 'NDArray', b: 'NDArray') -> 'NDArray'`
- `csub_format(a: 'Format', b: 'Format') -> 'Format'`
- `im_of(v: 'NDArray') -> 'NDArray'` — The imag stored-int component view of a structured complex array.
- `int_format(W: 'int', signed: 'bool' = True) -> 'Format'` — The :class:`Format` of an integer inner (``frac_bits == 0`` -> ``I == W``).
- `make_complex(re: 'NDArray', im: 'NDArray', fmt: 'Format') -> 'NDArray'` — Pack re/im stored-int arrays into a structured complex array of ``fmt``.
- `re_of(v: 'NDArray') -> 'NDArray'` — The real stored-int component view of a structured complex array.

### `waveflow.utils.cosimparse` — Parse Vitis HLS cosim reports.

**Classes:**
- `CosimReportParser` — Parse a Vitis HLS cosim report and extract the transaction cycle count.  _(methods: get_transaction_cycles)_

### `waveflow.utils.csynthparse`

**Classes:**
- `CsynthParser` — Class for parsing Vivado HLS synthesis reports (csynth.xml).  _(methods: get_loop_pipeline_info, get_module_resources, get_resources, get_total_resources)_

### `waveflow.utils.fixputils` — Integer-backed, Vitis-bit-exact fixed-point numeric core.

**Classes:**
- `Format` — A fixed-point format ``ap_fixed<W, I, q, o>`` (or ``ap_ufixed`` if unsigned).  _(methods: dtype, frac_bits)_
- `OMode` — Overflow mode (value = the Vitis ``ap_fixed`` template token).
- `QMode` — Quantization mode (value = the Vitis ``ap_fixed`` template token).

**Functions:**
- `add(sa: 'NDArray', a: 'Format', sb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'`
- `add_format(a: 'Format', b: 'Format') -> 'Format'` — ``ap_fixed`` add: align fractions (F = max), grow integer bits by 1 (carry).
- `fixed_sum(sa: 'NDArray', a: 'Format', axis: 'int | None' = None) -> 'tuple[NDArray, Format]'` — Full-precision reduction: sum stored ints, format grown by ceil(log2(N)).
- `mult(sa: 'NDArray', a: 'Format', sb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'`
- `mult_format(a: 'Format', b: 'Format') -> 'Format'` — ``ap_fixed`` mult: ``<Wa+Wb, Ia+Ib>`` (fraction bits add); signed if either.
- `quantize(stored: 'NDArray', src: 'Format', target: 'Format') -> 'NDArray'` — Requantize stored integers from ``src`` to ``target`` format (fixed -> fixed).
- `quantize_real(values: 'NDArray[np.float64] | float', fmt: 'Format') -> 'NDArray'` — Quantize real value(s) into ``fmt``'s stored integer (float -> fixed).
- `saturate(x: 'NDArray[np.int64] | int', wid: 'int' = 32, signed: 'bool' = True) -> 'NDArray[np.int64] | int'` — Saturate integers to the ``wid``-bit range (``AP_SAT``).
- `shift(sa: 'NDArray', a: 'Format', n: 'int') -> 'tuple[NDArray, Format]'` — Lossless point-move rescale: stored bits unchanged, value scaled by 2**n.
- `shift_format(a: 'Format', n: 'int') -> 'Format'` — Rescale by ``2**n`` (point-move): same W/bits, I grows by n, F shrinks by n.
- `sub(sa: 'NDArray', a: 'Format', sb: 'NDArray', b: 'Format') -> 'tuple[NDArray, Format]'`
- `sub_format(a: 'Format', b: 'Format') -> 'Format'` — ``ap_fixed`` sub: like add, but the result is always signed.
- `sum_format(a: 'Format', n_terms: 'int') -> 'Format'` — Reduction of ``n_terms`` values: grow integer bits by ``ceil(log2(n_terms))``.
- `to_bits(stored: 'NDArray | int', W: 'int') -> 'NDArray[np.uint64] | int'` — The raw ``W``-bit unsigned pattern (Vitis ``.range()``); two's-complement.
- `to_float(stored: 'NDArray | int', fmt: 'Format') -> 'NDArray[np.float64] | float'` — Real-valued view ``stored * 2**-F`` (exact for the v1 range).
- `truncate(x: 'NDArray[np.int64] | int', wid: 'int' = 32, signed: 'bool' = True) -> 'NDArray[np.int64] | int'` — Wrap integers to a ``wid``-bit two's-complement (``AP_WRAP``).

### `waveflow.utils.jsonutil` — Small JSON-serialization helpers shared by build scripts that write status /

**Functions:**
- `hex_word(value, bitwidth: 'int') -> 'str'` — Format ``value`` as a ``0x``-prefixed, zero-padded hex string of exactly
- `json_scalar(value)` — Coerce a numpy integer/floating scalar to a native Python scalar.

### `waveflow.utils.timing`

**Classes:**
- `ActivityDiagram` — Labeled horizontal lanes on a common cycle axis, showing *activity* rather than values.  _(methods: from_trace, occupancy_from_trace, plot, runs, set_occupancy)_
- `ClkSig` — Holds timing information for a single signal.  _(methods: clk_periods)_
- `SigTimingInfo` — Holds timing information for a single signal.
- `TimingDiagram`  _(methods: add_patch, add_signal, add_signals, plot_signals)_

### `waveflow.utils.trace` — Bind a trace manifest to a VCD and read per-channel bursts out of it.

**Classes:**
- `BoundTrace` — A manifest resolved against one VCD.  _(methods: aximm_bursts, channel, channel_blocked, channel_bursts, component, component_bursts, component_firings, port, port_bursts, task_done_cycles)_
- `ComponentView` — One component's observable surface, as seen from outside it.
- `Firing` — One firing of a component: ``start`` = its first input handshake, ``end`` = its ``ap_done``.  _(methods: span)_
- `TraceBindError` — A manifest named a net the VCD does not contain.

**Functions:**
- `load_trace(manifest: 'dict | str | Path', vcd_path: 'str | Path') -> 'BoundTrace'` — Resolve *manifest* against the VCD at *vcd_path*.

### `waveflow.utils.vcd`

**Classes:**
- `AximmBeatType` — Per-cycle data-phase status for an extracted AXI-MM burst.
- `AxisBurst` — One AXI4-Stream burst as a per-beat timeline.  _(methods: from_dict, n_beats, n_transfers)_
- `SigInfo` — Class to hold information about a VCD signal.  _(methods: get_values, set_format)_
- `VcdParser` — Class to parse VCD signals and extract information.  _(methods: add_aximm_signals, add_axiss_signals, add_clock_signal, add_signal, add_signals_prefix, add_sob_signals, add_status_signals, extract_aximm_bursts, extract_axis_bursts, extract_fifo_bursts, extract_sob_span, full_name, get_td_signals, get_values)_

**Functions:**
- `binary_str_to_numeric(bin_str: str, dtype: str, wid: int) -> int | float` — Converts a binary string to a numeric value of the specified type.
- `clock_sample_times(clk_times: numpy.ndarray, frac: float = 0.25) -> numpy.ndarray` — Sample points for reading synchronous signals, given the rising-edge times.
- `extract_clock_times(sig_info: waveflow.utils.vcd.SigInfo) -> list[float]` — Extracts the clock edge times from a VCD object for a given clock signal.
- `resample_signal(sig_info: waveflow.utils.vcd.SigInfo, clk_times: numpy.ndarray) -> numpy.ndarray` — Resamples a signal to new time points using nearest-neighbor interpolation.
- `split_framed_word(values: numpy.ndarray, data_width: int) -> tuple[numpy.ndarray, numpy.ndarray]` — Split ``streamutils::framed_word<W>`` samples into their data and ``last`` parts.
- `vcd_trace(trace_level: str) -> str` — Map a user trace level to the HLS cosim VCD trace pattern.
- `walk_handshake_bursts(data: numpy.ndarray, valid: numpy.ndarray, ready: numpy.ndarray, last: numpy.ndarray | None, clk_times: numpy.ndarray) -> list[dict]` — Walk one valid/ready channel and group its accepted beats into bursts.

